services:
  agent:
    container_name: claude-agent
    image: zerotymer/agent-isolation:latest-claude
    restart: unless-stopped
    user: "1000:1000"
    env_file:
      - path: .env
        required: false
    networks:
      - agent-net
    volumes:
      - .workspace:/workspace
      - agent-vol:/home/agent/


networks:
  agent-net:
    driver: bridge

volumes:
  agent-vol:
    driver: local
