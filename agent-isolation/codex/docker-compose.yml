services:
  agent:
    container_name: codex-agent
    image: zerotymer/agent-isolation:latest-codex
    restart: unless-stopped
    user: "1000:1000"
    env_file:
      - path: .env
        required: false
    networks:
      - agent-net
    volumes:
      - .workspace:/workspace
      - agent-vol:/home/agent/


networks:
  agent-net:
    driver: bridge

volumes:
  agent-vol:
    driver: local
