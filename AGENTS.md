# 저장소 기여 가이드

## 프로젝트 구조와 구성

이 저장소는 서로 독립적인 Docker 배포 구성을 모아 둔다. `nginx/`, `prometheus/`, `gitea/`, `wiki.js/` 같은 최상위 디렉터리 하나가 서비스 하나에 해당한다. 대부분 Compose 파일을 제공하며, 일부는 `docker run`을 실행하는 셸 스크립트도 포함한다. 서비스 설정은 해당 Compose 파일과 같은 디렉터리에 둔다. 예를 들어 Nginx 설정은 `nginx/config/`, Prometheus 설정은 `prometheus/`에 둔다. `.data/` 같은 실행 데이터는 예제 용도가 아니라면 추적하지 않는다.

## 실행 및 개발 명령어

저장소 전체를 대상으로 하는 빌드 명령은 없다. 작업할 서비스 디렉터리에서 다음 명령을 실행한다.

```sh
cd nginx
docker compose config                 # Compose 구성 파싱 및 검증
docker compose up -d                  # 백그라운드에서 서비스 시작
docker compose logs -f                # 시작 및 실행 로그 확인
docker compose down                   # 스택 중지 및 제거
```

도우미 스크립트가 있다면 포트와 볼륨 경로를 검토한 뒤 실행한다(예: `./prometheus.sh`). 이미지를 갱신할 때는 `docker compose pull`을 사용한다.

## 코딩 스타일과 이름 규칙

YAML은 공백 두 칸으로 들여쓰기하고 서비스와 디렉터리 이름은 소문자로 작성한다. 새 스택은 `docker-compose.yml`을 권장하되, 참조 경로를 함께 수정하지 않고 기존 파일명을 바꾸지 않는다. 모호한 YAML 값은 따옴표로 감싸고 가능한 경우 상대 경로로 바인드 마운트한다. 셸 스크립트에는 올바른 shebang을 사용하고 `"$(pwd)"`처럼 경로를 인용하며 Docker 옵션은 한 줄에 하나씩 작성한다. Python 유틸리티는 PEP 8과 `snake_case`를 따른다.

## 테스트 지침

자동 테스트나 커버리지 기준은 없다. 변경한 모든 스택에서 `docker compose config`를 실행한 뒤 서비스를 시작한다. `docker compose ps`와 `docker compose logs`로 컨테이너 상태, 공개 포트, 마운트 파일, 로그를 확인한다. 스모크 테스트에서 생성된 데이터는 커밋하지 않는다.

## 커밋과 Pull Request 지침

최근 커밋은 `gitea`, `heimdall`, `[fix]wiki.js`처럼 서비스 중심의 짧은 제목을 사용한다. 커밋 범위를 작게 유지하고 `nginx: exporter 상태 엔드포인트 활성화`처럼 대상과 작업을 명확히 적는다. 풀 리퀘스트에는 영향받는 서비스, 설정이나 포트 변경, 수행한 검증을 설명하고 관련 이슈를 연결한다. 화면 변경이 있을 때만 스크린샷을 첨부한다.

## 보안 및 설정 주의사항

`.env`, 비밀번호, API 키, 인증서, 호스트별 데이터는 커밋하지 않는다. 설정 안내가 필요하면 민감 정보를 제거한 예제를 제공한다. 재현성이 중요하거나 운영에 사용하는 배포는 이미지 버전을 고정하고, 실행 전 공개 포트와 쓰기 가능한 마운트를 검토한다.
